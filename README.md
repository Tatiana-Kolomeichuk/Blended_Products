# 🛒 Internet Shop Blended_Products

Навчальний командний проєкт: інтернет-магазин із трьома сторінками (**Home**, **Wishlist**, **Cart**) та модальним вікном продукту.  
Дані беруться з публічного API **DummyJSON**.

---

## 🔗 Demo

- GitHub Pages: *(додайте посилання після деплою)*

---

## ✅ Основний функціонал

### Home (index.html)
- Рендер списку категорій (`All` + категорії з API)
- Рендер товарів із пагінацією **12/сторінка**
- Кнопка **Load more** (ховається, коли товарів більше немає)
- Фільтрація товарів по категорії
- Пошук товарів за назвою
- Відкриття товару у модальному вікні (запит по `ID`)
- Додавання/видалення товару у **Wishlist** та **Cart**
- Лічильники Wishlist/Cart у навігації

### Wishlist (wishlist.html)
- Завантаження ID з `localStorage`
- Отримання товарів по `ID` через `Promise.all()`
- Рендер списку
- Модалка, додавання/видалення Wishlist/Cart

### Cart (cart.html)
- Завантаження ID з `localStorage`
- Отримання товарів по `ID` через `Promise.all()`
- Рендер списку
- Підрахунок:
  - Items (кількість товарів)
  - Total (загальна сума)
- Кнопка **Buy Products** 

---

## 🧩 Технології

- HTML / CSS / JavaScript (ES Modules)
- Vite (dev server + build)
- DummyJSON API

---

## 📦 API (DummyJSON)

Документація: https://dummyjson.com/docs/products

Ендпоінти:
- Products (pagination): `GET https://dummyjson.com/products?limit=12&skip=0`
- Product by ID: `GET https://dummyjson.com/products/:id`
- Search: `GET https://dummyjson.com/products/search?q=QUERY`
- Category list: `GET https://dummyjson.com/products/category-list`
- Products by category: `GET https://dummyjson.com/products/category/:category`

---

## 🗂️ Структура проєкту

Логіка розділена по модулях:

- `home.js` — логіка сторінки Home
- `wishlist.js` — логіка сторінки Wishlist
- `cart.js` — логіка сторінки Cart
- `products-api.js` — всі запити до бекенду
- `render-functions.js` — рендер розмітки (категорії, список товарів, модалка, тощо)
- `storage.js` — робота з `localStorage` (wishlist/cart)
- `handlers.js` — хендлери подій для `addEventListener`
- `modal.js` — відкриття/закриття модалки (backdrop / ESC / кнопка)
- `helpers.js` — допоміжні утиліти (пагінація, нормалізація, тощо)
- `refs.js` — посилання на DOM-елементи
- `constants.js` — константи 

---

## 💾 LocalStorage

Використовуються ключі:

- `wishlist` — масив ID товарів (наприклад: `[1, 5, 12]`)
- `cart` — масив ID товарів (наприклад: `[2, 9]`)

Поведінка:
- Якщо ID вже є в масиві → кнопка має показувати **Remove...**
- Якщо ID нема → **Add...**
- Після кожної зміни потрібно оновлювати лічильники в навігації.

---
